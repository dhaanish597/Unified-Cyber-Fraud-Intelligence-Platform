services:
  - type: web
    name: risk-engine-api
    env: docker
    region: ohio
    plan: free
    branch: deploy
    dockerContext: .
    dockerfilePath: api/Dockerfile
    envVars:
      - key: PORT
        value: 8000
