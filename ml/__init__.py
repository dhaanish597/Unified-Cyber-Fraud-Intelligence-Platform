# ml/__init__.py
"""
ml — Tabular fraud model + anomaly detection for the Unified Cyber-Fraud Intelligence Platform.

Public modules:
    ml.features  — feature engineering
    ml.predict   — inference (tabular_score, anomaly_score, score_transaction)
    ml.train     — training pipeline (run as __main__)
"""
